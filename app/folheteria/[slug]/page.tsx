export const dynamic="force-dynamic";import{notFound}from"next/navigation";import{Header}from"@/components/layout/Header";import{Footer}from"@/components/layout/Footer";import{ProductOfferCard}from"@/components/leaflets/ProductOfferCard";import{getLeaflet,getLeafletProducts}from"@/lib/leaflets";import styles from"./page.module.css";
export default async function LeafletPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const leaflet=await getLeaflet(slug);if(!leaflet)notFound();const products=await getLeafletProducts(leaflet.id);const sections=[...new Set(products.map(p=>p.section).filter((v):v is string=>Boolean(v)))];return <><Header/><main className={styles.main}><div className="shell"><header className={styles.header}><span>FOLHETERIA DIGITAL</span><h1>{leaflet.name}</h1><p>Ofertas válidas de {new Date(leaflet.starts_at+"T12:00:00").toLocaleDateString("pt-BR")} a {new Date(leaflet.ends_at+"T12:00:00").toLocaleDateString("pt-BR")}.</p></header>{sections.length?<div className={styles.categories}><span>Todos</span>{sections.map(s=><span key={s}>{s}</span>)}</div>:null}<div className={styles.grid}>{products.map(p=><ProductOfferCard product={p} key={p.id}/>)}</div></div></main><Footer/></>}
