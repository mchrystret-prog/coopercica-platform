import Image from "next/image";import type{LeafletProduct}from "@/lib/leaflets";import styles from "./ProductOfferCard.module.css";
const money=(v:number)=>v.toLocaleString("pt-BR",{minimumFractionDigits:2,maximumFractionDigits:2});
export function ProductOfferCard({product}:{product:LeafletProduct}){const hasCooper=(product.coopermais_price??0)>0;const hasOffer=(product.offer_all_price??0)>0;const featured=hasCooper?product.coopermais_price!:hasOffer?product.offer_all_price!:product.regular_price;const label=hasCooper?"COOPERMAIS":hasOffer?"OFERTA":"PREÇO";
return <article className={styles.card}><div className={styles.brand}><Image src="/images/logo.png" alt="Coopercica" width={150} height={26}/></div><div className={styles.visual}>{product.image_url?<img src={product.image_url} alt={product.description}/>:<div className={styles.noImage}>Imagem em breve</div>}{product.super_offer?<span className={styles.badge}>Super Oferta</span>:null}{product.buy_3_pay_2?<span className={styles.badge}>Leve 3 Pague 2</span>:null}</div><div className={styles.copy}><strong>{product.description}</strong>{product.complement?<span>{product.complement}</span>:null}</div><div className={styles.priceBox}>{(hasCooper||hasOffer)?<div className={styles.regular}><small>PREÇO REGULAR</small><b>R$ {money(product.regular_price)}</b></div>:null}{(hasCooper||hasOffer)?<span className={styles.divider}/>:null}<div className={styles.featured}><small>{label}</small><div><sup>R$</sup><b>{money(featured)}</b>{product.unit?<em>/{product.unit}</em>:null}</div></div></div>{product.promo_pack?<div className={styles.warnings}><span>{product.promo_pack}</span></div>:null}{product.age_18?<div className={styles.warnings}><span>Venda proibida para menores de 18 anos.</span></div>:null}</article>}
