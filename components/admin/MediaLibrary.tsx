"use client";import{useEffect,useState}from"react";import{SUPABASE_PUBLISHABLE_KEY,SUPABASE_URL}from"@/lib/leaflets";type O={name:string;id:string;metadata?:{mimetype?:string;size?:number}};export function MediaLibrary(){const[items,setItems]=useState<O[]>([]);const[folder,setFolder]=useState("site-content");useEffect(()=>{const t=sessionStorage.getItem("coopercica_admin_token");if(!t)return;fetch(`${SUPABASE_URL}/storage/v1/object/list/site-content`,{method:"POST",headers:{apikey:SUPABASE_PUBLISHABLE_KEY,Authorization:`Bearer ${t}`,"Content-Type":"application/json"},body:JSON.stringify({prefix:"",limit:100,sortBy:{column:"created_at",order:"desc"}})}).then(r=>r.ok?r.json():[]).then(setItems)},[]);return <div className="cms-list">{items.length?items.map(x=><article className="cms-list-row" key={x.id||x.name}><div><strong>{x.name}</strong><small>{x.metadata?.mimetype||"Pasta/arquivo"}</small></div></article>):<div className="admin-card"><h2>Biblioteca de mídia</h2><p>Os uploads de campanhas, revistas e lojas são organizados automaticamente no Storage. Novos arquivos entram aqui pelos respectivos módulos.</p></div>}</div>}